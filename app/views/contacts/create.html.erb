<% @page_title = "Contato - Sematter Sementes" %>
<% @content_title = "Contato" %>

<div>
  <% flash.each do |key, value| %>
    <div class="alert alert-success" role="alert">
        <%= value %>
    </div>
  <% end %>
</div>

<%= render 'contacts' %>
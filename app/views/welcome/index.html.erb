<!-- Carousel -->

<div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
  <% @banners.each_with_index do |banner, index| %>
            <%=
            if index == 0
                render inline: "<li data-target='#carousel-example-generic' data-slide-to='#{index}' class='active'></li>"
            else
                render inline: "<li data-target='#carousel-example-generic' data-slide-to='#{index}'></li>"
            end %>
        <% end %>
  </ol>
 
  <!-- Wrapper for slides -->
  <div class="carousel-inner">
 
        <% btn = image_path('bt-saiba-mais.png') %>
           <% @banners.each_with_index do |banner, index| %>
            <%=          
                if index == 0
                    render inline: "
                                    <div class='item active'>
                                    <a href='#{banner.link}'>
                                        <img src='#{banner.image.url(:original)}' alt='...'>
                                    </a>
                                    </div>
                    "
                else
                    render inline: "
                                    <div class='item'>
                                    <a href='#{banner.link}'>
                                        <img src='#{banner.image.url(:original)}' alt='...'>
                                    </a>
                                    </div>
                    "
                end %>
            <% end %>
  </div>
 
  <!-- Controls -->
  <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
  </a>
  <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
  </a>
</div> <!-- Carousel -->




<div id="qual">
<%= scroll_reveal animation: 'enter left move 50px' do %>
    <div class="container">
        <h2 class="text-center">QUAL É O RAIX INDICADO PARA O MEU USO?</h2>
    <div class="row">
        <div class="col-sm-4 col-sm-offset-4">
        <%= form_tag(products_path, :role => 'search', :method => :get) do %>
          <div class="form-group">
          <label class="control-label" for="data-raix">Data do plantio do Raix</label>
            <div class='input-group date datepicker'>                
                <%= text_field_tag 'data-raix', nil, placeholder: 'Data do plantio do Raix', :class => 'form-control datepickerinput' %>
                <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
            </div>
          </div>
          <div class="form-group">
          <label class="control-label" for="data-raix">Data do plantio do Raix</label>
            <div class='input-group date datepicker'>                
                <%= text_field_tag 'data-cultura', nil, placeholder: 'Data do plantio da sua cultura comercial', :class => 'form-control datepickerinput' %>
                <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
            </div>
          </div>
        <div class="form-group">
        <label class="control-label" for="cultivation">Cultura Comercial</label>
                    <select name="cultivation" class="form-control" id="cultivation">
                      <option value="">Cultivo</option>
                      <% Cultivation.all.collect do |p| %>
                      <option value="<%= p.id %>"><%= p.name %></option>
                      <% end %>
                    </select>
                  </div>
                 <%= button_tag(type: "submit", class: "btn btn-default") do %>
                    <span class="glyphicon glyphicon-zoom-in"></span> Buscar
                <% end %>
        <% end %>
        </div><!-- /col -->
    </div><!-- /row -->
    </div><!-- /container -->
    <% end %><!-- /scrollrevealjs -->
</div><!-- /qual -->

